#include <bits/stdc++.h>
using namespace std;
int main(){
vector<int> arr; //if it's dynamic input , vector<int > a(n);
    int x;
    while(cin >> x){
        arr.push_back(x);
    }
}
// vector<int> a(n);                    // 1D vector of size n
// vector<int> a(n, 5);                 // size n, all values = 5

// vector<vector<int>> A(n);            // n rows, 0 columns initially
// vector<vector<int>> A(n, vector<int>(m)); // n × m matrix

// vector<string> s(n);                 // n strings