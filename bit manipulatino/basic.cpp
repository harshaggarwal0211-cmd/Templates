#include <bits/stdc++.h>
using namespace std;
#define int long long 
/* (^,xor) --> if a==b return 0 ;
    (|,or) , (&,and) 
    ( >> , right shift) a>>n == a/2^n
    ( << , left shift ) a<<n == a*2^n , shift whole bit to left , add zero in right
    (~ , negation)
    ( - , 2's compliment)

*/
signed main(){

}